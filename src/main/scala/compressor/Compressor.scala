package compressor

object Compressor {

  /**
    * Split into
    * @param fileData
    * @return
    */
  def compress(fileData: String): String = {
    fileData
  }

  def decompress(compressedFileData: String): String = {
    compressedFileData
  }
}
